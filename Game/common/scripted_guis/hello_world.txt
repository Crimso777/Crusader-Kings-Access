		hello_world = {

	scope = character

	saved_scopes = { 
} 		# any additional targets
	is_valid = {} 		# can the player use it?

	effect = {			# what it does
	debug_log = "<out>Hello World!</out>"


}
}
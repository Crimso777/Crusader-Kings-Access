speak_registry = {

	scope = character

	saved_scopes = { 
} 		# any additional targets
	is_valid = {} 		# can the player use it?

	effect = {			# what it does
		debug_log = "<out>arg0:[GetGlobalVariable('arg0').GetValue]</out>"
}
}